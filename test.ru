kaam calculateFib @adad (n @ adad |c@adad) karo rakho x@adad :=0;
	rakho y@adad:= 9808;
	z@adad;
    i@adad := 1;
    num @ adad;
	result@adad := chalao calculateFib(arg1|arg2);
    z :=xvar+yvar;
    x:=y;
    y := z;
    i := i +12 ;

    dekhao<<`munfi number daal diya\n`;
    dekhao << `yar 0 hai!\n` ;
    dekhao<<x<<` `;
	
    agar(n<0)to phir karo
	warna agar (n = 0) to phir
	warna phir dekhao << `\nfibonacci series: `;
    
	lo<< `Enter the number`>> num; input
    lo >>variableName@adad;
    lo>> variableName;

    jab tak (i-1 >= n) karo ; ye loop hay
    bas karo

    wapas bhaijo 0 ;
	wapas bhaijo xf; halt
kaam khatam

;fibonacci series ka hisaab karnay and dekhanay wala program
kaam markazi@ khali() karo

;error
;123a
;.

;id
;asd_123
;__
;_234sdf

;str
;''
;`%#$sdf_32490fg @ `